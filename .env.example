# =============================================================================
# python-krtour-map .env (예시)
#   - 실제 .env 파일은 권한 600 또는 systemd EnvironmentFile/vault에 둔다.
#   - 평문 커밋 금지. SecretStr로 settings에 로드한다.
# =============================================================================

# ----- Postgres / PostGIS -----
KRTOUR_MAP_PG_DSN=postgresql+asyncpg://krtour_map:CHANGE_ME@localhost:5432/krtour_map
# asyncpg와 분리된 sync driver가 필요한 도구(Alembic 등)는 다음을 추가로 정의 가능
# KRTOUR_MAP_PG_DSN_SYNC=postgresql+psycopg://krtour_map:CHANGE_ME@localhost:5432/krtour_map
KRTOUR_MAP_PG_POOL_SIZE=10
KRTOUR_MAP_PG_MAX_OVERFLOW=10
KRTOUR_MAP_PG_POOL_PRE_PING=true

# ----- 디버그 REST API (옵션, 인증 없음, 내부망 전용) -----
KRTOUR_MAP_DEBUG_API_HOST=127.0.0.1
KRTOUR_MAP_DEBUG_API_PORT=8600

# ----- 객체 저장소 (S3 호환; RustFS 우선, MinIO/Ceph/R2 swap 가능) -----
KRTOUR_MAP_OBJECT_STORE_ENDPOINT_URL=http://127.0.0.1:9000
KRTOUR_MAP_OBJECT_STORE_BUCKET=krtour-map
KRTOUR_MAP_OBJECT_STORE_REGION=us-east-1
KRTOUR_MAP_OBJECT_STORE_ACCESS_KEY_ID=CHANGE_ME
KRTOUR_MAP_OBJECT_STORE_SECRET_ACCESS_KEY=CHANGE_ME
KRTOUR_MAP_OBJECT_STORE_PUBLIC_BASE_URL=http://127.0.0.1:9000/krtour-map

# ----- 주소/좌표 -----
# python-kraddr-geo 로컬 DB DSN (선택 — 미설정 시 AsyncAddressClient 기본 .env 사용)
# KRTOUR_MAP_KRADDR_GEO_PG_DSN=postgresql+asyncpg://kraddr:CHANGE_ME@localhost:5432/kraddr_geo

# ----- Provider API 키 (각 provider 라이브러리가 직접 읽음, 본 .env는 reference) -----
# KMA
KMA_API_KEY=
# VisitKorea / TourAPI
VISITKOREA_SERVICE_KEY=
# 국가유산청
KRHERITAGE_API_KEY=
# 산림청
KRFOREST_API_KEY=
# 고속도로공사
KREX_API_KEY=
# 해양조사원
KHOA_API_KEY=
# AirKorea
AIRKOREA_API_KEY=
# OpiNet
OPINET_API_KEY=
# data.go.kr 표준데이터 (우선순위 순)
DATAGOKR_API_KEY=
DATA_GO_KR_SERVICE_KEY=
PUBLIC_DATA_SERVICE_KEY=
SERVICE_KEY=

# ----- 전화번호 보강 (Place phone enrichment, 선택) -----
KAKAO_LOCAL_REST_API_KEY=
NAVER_SEARCH_CLIENT_ID=
NAVER_SEARCH_CLIENT_SECRET=
GOOGLE_PLACES_API_KEY=

# ----- Sentry (선택, TripMate가 주입하는 게 표준) -----
# KRTOUR_MAP_SENTRY_DSN=

# ----- 로그 -----
KRTOUR_MAP_LOG_LEVEL=INFO
KRTOUR_MAP_LOG_FORMAT=json   # structlog JSON; stdout 직출력 (Promtail 자동 수집)
