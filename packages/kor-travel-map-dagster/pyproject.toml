[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kor-travel-map-dagster"
version = "0.2.0-dev"
description = "Dagster code location for kor-travel-map standalone ETL."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [{ name = "digitie" }]
keywords = ["kor-travel-map", "dagster", "etl", "postgis"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = [
  "kor-travel-map==0.2.0-dev",
  "dagster>=1.9,<2",
  "dagster-webserver>=1.9,<2",
  "dagster-postgres>=0.25,<1",
  "boto3>=1.34,<2",
  "botocore>=1.34,<2",
  "httpx>=0.27,<1.0",
  # mois source sync가 provider에 넘기는 session은 `AsyncSession`이어야 한다
  # (provider가 `await session.run_sync(...)`를 부른다). 그 engine이
  # `sqlite+aiosqlite`다. `mois` 자신도 선언하지만 이 경로를 여기서 만들므로
  # 이 패키지도 직접 선언한다 — 대역으로 도는 테스트에는 `mois`가 없다.
  "aiosqlite>=0.20",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["kortravelmap.dagster*"]
namespaces = true

[tool.setuptools.package-data]
"kortravelmap.dagster" = ["py.typed"]

[tool.dagster]
module_name = "kortravelmap.dagster.definitions"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "PIE", "PT", "RET", "SIM", "TID"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
